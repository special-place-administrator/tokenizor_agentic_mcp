---
phase: 01-liveindex-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/mod.rs
  - src/domain/index.rs
  - src/error.rs
  - src/lib.rs
  - src/parsing/mod.rs
  - Cargo.toml
autonomous: true
requirements:
  - RELY-04
must_haves:
  truths:
    - "cargo check passes with zero errors after v1 module deletion"
    - "src/parsing/ module compiles without crate::storage dependency"
    - "Domain types (LanguageId, SymbolRecord, SymbolKind, FileOutcome, FileProcessingResult) exist with minimal derives (no JsonSchema, no serde on types that don't need it yet)"
    - "Cargo.toml has no v1-only dependencies (fs2, spacetimedb-sdk, schemars, num_cpus, tokio-util)"
    - "rayon is in Cargo.toml as a dependency"
    - "stdout purity: tracing writes to stderr only (observability.rs preserved)"
  artifacts:
    - path: "src/domain/mod.rs"
      provides: "Slim domain module re-exporting only kept types"
      contains: "pub use index"
    - path: "src/domain/index.rs"
      provides: "LanguageId, SymbolRecord, SymbolKind, FileOutcome, FileProcessingResult stripped to minimal derives"
      exports: ["LanguageId", "SymbolRecord", "SymbolKind", "FileOutcome", "FileProcessingResult"]
    - path: "src/error.rs"
      provides: "Simplified TokenizorError for v2 (no Storage, ConflictingReplay, Integrity, ControlPlane, RequestGated, Serialization)"
      exports: ["TokenizorError", "Result"]
    - path: "src/lib.rs"
      provides: "Crate root declaring kept modules (domain, parsing, error, observability) and new empty modules (live_index, discovery)"
    - path: "Cargo.toml"
      provides: "Cleaned dependency list with rayon added"
  key_links:
    - from: "src/parsing/mod.rs"
      to: "src/domain/index.rs"
      via: "use crate::domain::{FileOutcome, FileProcessingResult, LanguageId, SymbolRecord}"
      pattern: "use crate::domain"
    - from: "src/lib.rs"
      to: "src/domain/mod.rs"
      via: "pub mod domain"
      pattern: "pub mod domain"
---

<objective>
Tear down all v1 modules except the kept files, strip domain types to minimal derives, fix the parsing module's broken import, clean up Cargo.toml, and establish the new module skeleton so `cargo check` passes.

Purpose: Every subsequent plan depends on a compiling codebase. This plan creates the clean foundation by removing ~20,000 lines of v1 infrastructure and establishing the v2 module structure.

Output: A compiling codebase with `src/parsing/`, stripped `src/domain/`, rewritten `src/error.rs` and `src/lib.rs`, cleaned `Cargo.toml`, and empty module stubs for `live_index` and `discovery`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-liveindex-foundation/01-CONTEXT.md
@.planning/phases/01-liveindex-foundation/01-RESEARCH.md

@src/parsing/mod.rs
@src/parsing/languages/mod.rs
@src/domain/index.rs
@src/domain/mod.rs
@src/error.rs
@src/observability.rs
@src/lib.rs
@Cargo.toml

<interfaces>
<!-- Types that src/parsing/mod.rs imports and MUST continue to find after restructure -->

From src/domain/index.rs (v1, lines 99-142 — these types are KEPT but stripped):
```rust
pub struct FileProcessingResult {
    pub relative_path: String,
    pub language: LanguageId,
    pub outcome: FileOutcome,
    pub symbols: Vec<SymbolRecord>,
    pub byte_len: u64,
    pub content_hash: String,  // NOTE: keep field for now, parsing still computes it
}

pub enum FileOutcome {
    Processed,
    PartialParse { warning: String },
    Failed { error: String },
}

pub struct SymbolRecord {
    pub name: String,
    pub kind: SymbolKind,
    pub depth: u32,
    pub sort_order: u32,
    pub byte_range: (u32, u32),
    pub line_range: (u32, u32),
}

pub enum SymbolKind {
    Function, Method, Class, Struct, Enum, Interface,
    Module, Constant, Variable, Type, Trait, Impl, Other,
}
```

From src/storage/sha256.rs (v1, DELETED — function must be relocated):
```rust
pub fn digest_hex(bytes: &[u8]) -> String  // SHA-256 hex digest, hand-rolled
```

From src/parsing/mod.rs (v1, line 8 — this import BREAKS after storage deletion):
```rust
use crate::storage::digest_hex;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete v1 modules, clean Cargo.toml, fix parsing import</name>
  <files>
    src/application/ (DELETE entire directory)
    src/config.rs (DELETE)
    src/indexing/ (DELETE entire directory)
    src/protocol/ (DELETE entire directory)
    src/storage/ (DELETE entire directory — but FIRST extract sha256.rs digest_hex function)
    src/domain/context.rs (DELETE)
    src/domain/health.rs (DELETE)
    src/domain/idempotency.rs (DELETE)
    src/domain/init.rs (DELETE)
    src/domain/migration.rs (DELETE)
    src/domain/registry.rs (DELETE)
    src/domain/repository.rs (DELETE)
    src/domain/retrieval.rs (DELETE)
    src/domain/workspace.rs (DELETE)
    tests/epic4_hardening.rs (DELETE)
    tests/fixtures/ (DELETE entire directory)
    tests/indexing_integration.rs (DELETE)
    tests/retrieval_integration.rs (DELETE)
    src/parsing/mod.rs (MODIFY — replace digest_hex import)
    Cargo.toml (MODIFY — remove v1 deps, add rayon)
  </files>
  <action>
  **Step 1: Extract digest_hex before deleting storage.**
  Read `src/storage/sha256.rs` and copy the `digest_hex` function (and its helper `digest`, `to_hex`, and the K constant array). Create a new file `src/hash.rs` containing these functions as `pub(crate)`. This is a pure SHA-256 implementation with no external deps — keep it verbatim.

  **Step 2: Delete all v1 modules.**
  Delete the following directories entirely: `src/application/`, `src/indexing/`, `src/protocol/`, `src/storage/`.
  Delete `src/config.rs`.
  Delete all domain submodules EXCEPT `index.rs`: delete `context.rs`, `health.rs`, `idempotency.rs`, `init.rs`, `migration.rs`, `registry.rs`, `repository.rs`, `retrieval.rs`, `workspace.rs`.
  Delete test files: `tests/epic4_hardening.rs`, `tests/fixtures/` (entire directory), `tests/indexing_integration.rs`, `tests/retrieval_integration.rs`.
  KEEP: `src/parsing/` (entire), `src/domain/index.rs`, `src/observability.rs`, `src/error.rs` (will rewrite), `src/lib.rs` (will rewrite), `src/main.rs` (will be rewritten in Plan 03), `tests/tree_sitter_grammars.rs`, `tests/retrieval_conformance.rs`, `npm/` (entire).

  **Step 3: Fix parsing import.**
  In `src/parsing/mod.rs`, replace `use crate::storage::digest_hex;` with `use crate::hash::digest_hex;`. No other changes to parsing — the function signature is identical.

  **Step 4: Clean Cargo.toml.**
  Remove dependencies: `fs2`, `spacetimedb-sdk`, `schemars`, `num_cpus`, `tokio-util`.
  Add dependency: `rayon = "1.10"`.
  Keep everything else (anyhow, rmcp, serde, serde_json, thiserror, tokio, tracing, tracing-subscriber, all tree-sitter-* crates, ignore).
  Note: `serde` and `serde_json` are kept because `rmcp` needs them and Phase 2 MCP tools will need them. `rmcp` is kept because `main.rs` will need it in Phase 2 — removing and re-adding is churn.
  </action>
  <verify>
    <automated>cd /c/AI_STUFF/PROGRAMMING/tokenizor_agentic_mcp && ls src/application src/indexing src/protocol src/storage src/config.rs 2>&1 | grep -c "No such file" | grep -qx "5"</automated>
  </verify>
  <done>All v1 directories and files deleted. digest_hex relocated to src/hash.rs. Parsing import updated. Cargo.toml cleaned.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite domain/mod.rs, domain/index.rs, error.rs, lib.rs — establish v2 module skeleton</name>
  <files>
    src/domain/mod.rs
    src/domain/index.rs
    src/error.rs
    src/lib.rs
    src/live_index/mod.rs (CREATE — empty stub)
    src/discovery/mod.rs (CREATE — empty stub)
    src/hash.rs (already created in Task 1)
  </files>
  <action>
  **Step 1: Rewrite src/domain/index.rs — strip to minimal derives.**
  Keep ONLY: `LanguageId` (with `Clone, Debug, PartialEq, Eq, Hash, PartialOrd, Ord` — drop `Serialize, Deserialize, JsonSchema, serde rename_all`), `SymbolRecord` (keep `Clone, Debug, PartialEq, Eq` — drop `Serialize, Deserialize`), `SymbolKind` (keep `Clone, Copy, Debug, PartialEq, Eq` — drop `Serialize, Deserialize, JsonSchema, serde rename_all`), `FileOutcome` (keep `Clone, Debug, PartialEq, Eq` — drop `Serialize, Deserialize, serde rename_all`), `FileProcessingResult` (keep `Clone, Debug, PartialEq, Eq` — drop `Serialize, Deserialize`), `SupportTier` (keep `Clone, Debug, PartialEq, Eq` — drop `Serialize, Deserialize, serde rename_all`).
  Keep ALL the impl blocks for `LanguageId` (`from_extension`, `extensions`, `support_tier`, `Display`).
  Delete everything else from `index.rs`: `IndexRun`, `IndexRunMode`, `IndexRunStatus`, `Checkpoint`, `DiscoveryManifest`, `FileOutcomeSummary`, `FileRecord`, `PersistedFileOutcome`, `RunPhase`, `RunHealth`, etc.
  Remove `use schemars::JsonSchema;` and `use serde::{Deserialize, Serialize};` imports (and the `BTreeMap` import if no longer needed).
  Remove `use super::repository::RepositoryStatus;` and `use super::retrieval::NextAction;` imports.

  **Step 2: Rewrite src/domain/mod.rs.**
  Replace entire file with:
  ```rust
  pub mod index;

  pub use index::{
      FileOutcome, FileProcessingResult, LanguageId, SupportTier,
      SymbolKind, SymbolRecord,
  };
  ```

  **Step 3: Rewrite src/error.rs — simplify for v2.**
  Keep: `TokenizorError` enum with variants: `Io { path, source }`, `Parse(String)`, `Discovery(String)`, `CircuitBreaker(String)`, `Config(String)`.
  Keep: `pub type Result<T> = std::result::Result<T, TokenizorError>;`
  Keep: `io()` constructor helper.
  Drop variants: `Storage`, `ConflictingReplay`, `Integrity`, `ControlPlane`, `Serialization`, `InvalidArgument`, `InvalidOperation`, `NotFound`, `RequestGated`.
  Drop: `is_systemic()` method, `From<serde_json::Error>` impl.
  Add: `From<std::io::Error>` impl for convenience.

  **Step 4: Rewrite src/lib.rs.**
  Replace entire file with module declarations for the v2 structure:
  ```rust
  pub mod domain;
  pub mod error;
  pub mod hash;
  pub mod observability;
  pub mod parsing;
  pub mod live_index;
  pub mod discovery;
  ```

  **Step 5: Create empty module stubs.**
  Create `src/live_index/mod.rs` with a comment: `// LiveIndex store and query — implemented in Plan 02`
  Create `src/discovery/mod.rs` with a comment: `// File discovery — implemented in Plan 02`

  **Step 6: Verify compilation.**
  Run `cargo check`. If `tests/tree_sitter_grammars.rs` or `tests/retrieval_conformance.rs` fail to compile due to removed types, fix their imports. `tree_sitter_grammars.rs` likely only imports from `src/parsing/` so it should be fine. `retrieval_conformance.rs` likely imports deleted v1 types — if it does not compile, stub it out with `#[cfg(feature = "v1")]` or comment out the broken parts with a `// TODO: update for v2 types in Phase 2` note, keeping the file per user decision.
  </action>
  <verify>
    <automated>cd /c/AI_STUFF/PROGRAMMING/tokenizor_agentic_mcp && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>cargo check passes with zero errors. Domain types have minimal derives. Error types simplified. Module skeleton declares live_index and discovery as empty stubs. No println! or stdout writes anywhere in remaining code.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes with zero errors and zero warnings about unused imports from deleted modules
2. `ls src/` shows only: domain/, discovery/, error.rs, hash.rs, lib.rs, live_index/, main.rs, observability.rs, parsing/
3. `grep -r "println!" src/` returns zero hits (stdout purity baseline)
4. `grep -r "schemars" src/` returns zero hits (JsonSchema removed)
5. `grep -r "spacetimedb" Cargo.toml` returns zero hits
6. `cargo test --lib parsing` passes (parsing module tests still work)
</verification>

<success_criteria>
- `cargo check` succeeds
- All v1 modules deleted (application, config, indexing, protocol, storage, domain submodules except index.rs)
- Domain types stripped to minimal derives
- `src/parsing/mod.rs` compiles with `crate::hash::digest_hex` import
- Cargo.toml has rayon, does NOT have fs2/spacetimedb-sdk/schemars/num_cpus/tokio-util
- Empty stubs exist for `src/live_index/mod.rs` and `src/discovery/mod.rs`
- `cargo test --lib parsing` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-liveindex-foundation/01-01-SUMMARY.md`
</output>
